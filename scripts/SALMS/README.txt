The package SALMS (Signal analyses for land monitoring systems) 
includes a set of function that can be used to analyse remote sensing signal behaviour.

The package can be installed directly in R with:
devtools:::install_github("ec-jrc/cbm/scripts/SALMS", build_vignettes = TRUE)
This will also install the package vignette, which gives an introduction to the functionality of the package.

